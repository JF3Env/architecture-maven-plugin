import java.nio.file.Files;
String report = Files.readString(new File(basedir, "target/architecture/source-report.txt").toPath());
if (!report.contains("SOURCE_INVENTORY | ") || !report.contains("unexpected ownership package consumer.example.orders.domain")) {
    throw new IllegalStateException("Wrong scope must not pass an empty analysis: " + report);
}
return true;
